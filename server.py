# create a server socket
# bind server socket to a specific port and address
# listen for incoming connections
# if any connection , accept the client connection and receive data from the client

surname = "Mwangi"
name = "Nelson {surname}"

print(name)